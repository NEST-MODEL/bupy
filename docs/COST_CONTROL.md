# Контроль расходов

Цель: **никогда не платить и не привязывать карту.**

## Используемые сервисы (тариф Spark)

| Сервис | Использование | Замечание |
|---|---|---|
| Firebase Authentication | Email/Password | Бесплатно. Не включайте телефонную (SMS) авторизацию — она платная. |
| Cloud Firestore | Данные пользователей | Бесплатная суточная квота: 50 000 чтений, 20 000 записей, 20 000 удалений, 1 ГиБ хранения (актуальные цифры смотрите на странице Firebase Pricing). |
| Firebase Hosting | Необязательно | Бесплатный лимит Spark. Альтернативы: Cloudflare Pages, GitHub Pages. |

На Spark при исчерпании квоты операции просто начинают отклоняться до следующих суток — автоматических списаний нет, потому что нет привязанной карты.

## Что расходует квоту Firestore

- Каждый документ в результате запроса = 1 чтение (список из 50 записей = 50 чтений).
- Открытие страницы с realtime-подпиской перечитывает данные при каждом подключении.
- Правила с `get()`/`exists()` добавляют чтения — поэтому в наших правилах их нет.
- Офлайн-кеш уменьшает повторные чтения.

## Как не превысить квоту

- Загружать данные питомца по требованию, а не подписываться на все коллекции сразу.
- Использовать `limit()` и пагинацию для длинных историй.
- Realtime-слушатели — только там, где нужно живое обновление.
- Хранить фото питомца в сжатом виде (≤ ~60 КБ) — документ Firestore не должен разрастаться.
- Следить за Usage в Firebase Console → Firestore → Usage.

## Что отключено или изменено из-за возможной оплаты

- **Cloud Storage for Firebase** — требует Blaze с 3 февраля 2026. Не используется.
- **Cloud Functions** — требуют Blaze. Не используются; напоминания локальные.
- **Платные Google Maps/OCR/SMS/email/AI API** — не используются.

## Правило на будущее

Если в консоли Firebase появится предложение «Upgrade to Blaze» ради какой-либо функции — отказаться и упростить/убрать функцию.

> Этап 4 (по решению автора проекта): рассматривается бесплатный Gemini API для Bupy Help. Перед подключением нужно проверить: не требует ли выбранный способ карту, и как защитить ключ — ключ, вставленный во frontend, виден любому пользователю, а серверного прокси без Blaze у нас нет.

## Другие бесплатные сервисы этапа 4

| Сервис | Использование | Лимиты и заметки |
|---|---|---|
| Nominatim (OpenStreetMap) | Геокодинг города в разделе «Ветклиники» | Бесплатный публичный сервис с вежливой политикой использования — не для тяжёлой нагрузки. Bupy делает один запрос по нажатию кнопки, без автозапросов при наборе текста. |
| Overpass API | Поиск ветклиник рядом | Тоже бесплатный, публичный, с ограничением по нагрузке. Один запрос на поиск, радиус ограничен (8 км), без фоновых опросов. |
| Tesseract.js (OCR паспорта) | Распознавание фото паспорта | Работает локально в браузере (WASM), но языковые данные при первом использовании скачиваются с бесплатного CDN — разовый трафик, дальше кешируется браузером. |
| Gemini API (бесплатный) | Свободные вопросы в Bupy Help + распознавание паспорта | По умолчанию используется **общий ключ проекта** (`VITE_GEMINI_API_KEY`) — лимит общий на всех пользователей сразу; при желании пользователь может подключить свой личный ключ в Профиле вместо общего. См. `GEMINI_SETUP.md` про компромиссы такого выбора. Без ключа Bupy Help работает по статической базе, OCR — через локальный Tesseract.js. |

## Важно про сеть в СНГ/Казахстане

Firestore по умолчанию использует потоковый транспорт (gRPC/WebChannel), который на части провайдеров и в некоторых сетях работает медленно или зависает на несколько минут, пока клиент сам не поймёт, что нужно переключиться на резервный режим. В `src/firebase/firebase.ts` включена опция `experimentalAutoDetectLongPolling: true`, которая заставляет Firestore сразу проверить транспорт и при необходимости использовать long polling — это устраняет многоминутные зависания при открытии страниц. Это бесплатная настройка самого клиента Firestore, никаких дополнительных сервисов не требует.
